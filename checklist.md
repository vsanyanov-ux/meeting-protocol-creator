# 🏁 Инженерный Чек-лист готовности проекта "Протоколист"

Этот документ — финальный фильтр качества. Пункт считается выполненным (`[x]`), только если пройден указанный **Метод проверки**.

---

## 🛠 1. Основной функционал (Core)
| Пункт | Метод проверки | Статус |
| :--- | :--- | :---: |
| **Мультиформатность** | Запуск `pytest backend/tests/test_normalizer.py` (все 5+ тестов OK) | [x] |
| **Качество STT** | Прогон `test_e2e_pipeline.py` с реальным аудио-файлом | [ ] |
| **Генерация протокола** | Проверка структуры в `test_protocol_gen.py` (наличие всех секций) | [ ] |
| **Экспорт в DOCX** | Ручная проверка: файл открывается в MS Word и LibreOffice без ошибок | [ ] |

---

## 🤖 2. AI-Аудитор (Critical)
| Пункт | Метод проверки | Статус |
| :--- | :--- | :---: |
| **Полный вывод текста** | Тест `test_robustness.py`: ответ аудитора > 1000 токенов является валидным JSON | [ ] |
| **Объективность оценки** | Сравнение `result['scores']` с эталонными значениями в `test_providers.py` | [ ] |
| **Отображение в UI** | Визуальный тест: блок аудитора не "расползается" при длинном тексте | [ ] |

---

## ✨ 3. Дизайн и UI/UX (Premium Experience)
| Пункт | Метод проверки | Статус |
| :--- | :--- | :---: |
| **WOW-эффект** | Субъективное ревью: интерфейс выглядит "дорого" (шрифты, отступы, тени) | [ ] |
| **Анимации** | Проверка `transition` и `hover` эффектов в CSS через Chrome DevTools | [ ] |
| **Реактивность** | WebSocket/Polling тест: смена статусов в UI происходит мгновенно | [ ] |
| **Адаптивность** | Проверка в DevTools: корректный вид на iPhone 14 и iPad Air | [ ] |

---

## 🔒 4. Надежность и Безопасность
| Пункт | Метод проверки | Статус |
| :--- | :--- | :---: |
| **Обработка ошибок** | Тест `test_robustness.py`: эмуляция сбоя API → получение 500/503 с описанием | [ ] |
| **Таймауты** | Проверка в `main.py`: `asyncio.wait_for` установлен на 600 сек | [ ] |
| **Очистка данных** | После теста: папки `temp_protocols` и `uploads` пустые (кроме `.gitkeep`) | [ ] |
| **Валидация** | Попытка загрузки 501 МБ → ответ `413 Payload Too Large` | [ ] |

---

## 🐳 5. Deployment & Environment
| Пункт | Метод проверки | Статус |
| :--- | :--- | :---: |
| **Docker Build** | Команда `docker-compose build` завершается без ошибок за < 5 мин | [ ] |
| **Конфигурация** | Прохождение `test_providers.py` при разных `AI_PROVIDER` в `.env` | [ ] |
| **Логирование** | Наличие записей `INFO:task_id...` в `backend/app.log` после прогона | [ ] |
| **Зависимости** | Команда `pip check` в контейнере не выдает конфликтов версий | [ ] |

---

## 🏁 Финальный аккорд
- [ ] Проект успешно обработал "боевое" совещание (30 мин) от загрузки до email.
- [ ] **Все пункты в таблицах выше отмечены как [x]**.
